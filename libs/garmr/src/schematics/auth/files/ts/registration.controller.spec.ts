class MockRegistrationService {
  public register = jest.fn()
}

describe("RegistrationController", () => {
  beforeEach(() => {})
  describe("register", () => {
    it("it should create a new registration", () => {})
  })
})
